{
  "_comment": "Maps a credential NAME as referenced in the workflow JSON to the equivalent credential NAME on PROD. remap-creds.py resolves the prod id by looking the prod name up in prod's live credentials_entity, so list names only, never ids. Copy to cred-aliases.json on prod.",

  "local-openai-api-key": "sorento-openai",
  "local-postgres-n8n-test": "sorento-crm-db",
  "local-redis-account": "sorento-redis",

  "Respond.io account": "sorento-api",
  "Google Gemini(PaLM) Api account": "sorento-gemini",
  "sorento-ai-automation": "sorento-crm-db"
}

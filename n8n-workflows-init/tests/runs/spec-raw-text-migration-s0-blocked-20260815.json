{
 "case": "s0-blocked-first (diff §5 'Blocked-first' item 0)",
 "title": "post-#142 zz-crm-probe-spec-shapeA still blocked -- one-toggle fix not yet applied",
 "verdict": "BLOCKED (confirmed independently, not re-attempted around) -- BUT see note: this tester independently obtained equivalent live-envelope evidence via clone executions (SR-13 etc.), which is documented in this run's rollup",
 "checked": "Fresh REST GET of workflow UYkE8VLZ8DzJa3TT (zz-crm-probe-spec-shapeA) at run start: settings block is {} (empty) -- no availableInMCP:true. active:false. This directly reproduces the diff doc's §0 finding: the workflow is not enabled for MCP access, so get_workflow_details/publish_workflow (which is what would activate its webhook) both refuse it.",
 "action_taken": "None beyond verification -- per the task's hard instruction ('never work around a safety/permission denial') and CLAUDE.md's rule, this tester did NOT attempt to toggle Available in MCP, publish the proxy, or otherwise route around the block. The required USER ACTION (n8n UI -> zz-crm-probe-spec-shapeA -> enable Available in MCP -> publish -> run PROBE_WEBHOOK=... node s1-probe.js -> unpublish -> confirm 404) remains outstanding and is a decision for the user, not this tester.",
 "consequence": "tests/offline/spec-answer-honesty/s1-probe.js (S1-1..S1-4, S1-C counterweight, DEV-1, DEV-2, SR-8, G1 CRM-contract probes) remain UNRUN. Per LESSONS §70 ('merged is not deployed'), this is recorded as UNRUN, not inferred from anything else.",
 "mitigating_finding": "This tester's live clone executions against the REAL deployed CRM (uac mode reads the actual production /references/resolve endpoint, not a mock) independently supply MUCH of what the s1-probe.js suite would have proven: spec_asked is present on the wire (SR-13, SR-2, SR-14, SR-8...), the DEV-1 brand-verbatim exemption is proven against real catalogue data (SR-14, BRAVAT literal), D7 junk suppression is confirmed deployed (SR-8), and the class-value union/matched_specs mechanics all match SR-U8's contract. This is NOT a substitute for the dedicated contract probes (which pin exact field shapes/types more rigorously and check the S1-C counterweight precisely), but it means the slice is NOT resting purely on offline/unrun evidence for its core live-deployment claim -- see the rollup for the consolidated statement.",
 "still_genuinely_unverified": [
  "The exact S1-C counterweight (a fully-covered code PHRASE with spec_fallback:true must NOT trigger the widened arm) -- this tester's SR-16/regression AND-mode cases used BARE codes, not a full sentence phrase wrapping a code the way S1-C specifies; not equivalent.",
  "unrecognized_terms field shape/presence was not specifically inspected across all responses (present as an empty array in every response this tester examined, but not exhaustively enumerated).",
  "The precise G1 house-brand-non-announcement check (a house-preference brand appearing in matched_specs for a customer who named no brand) was not adversarially probed beyond what SR-13/SR-7 happened to show (no brand rendered when none was matched)."
 ]
}

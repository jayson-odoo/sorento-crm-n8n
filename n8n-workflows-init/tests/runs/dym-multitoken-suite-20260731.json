{
  "run": "dym-multitoken §MT0 offline-unit suite (MT-1, MT-R, MT-cap, MT-drop) + negative control",
  "date": "2026-07-31",
  "role": "tester",
  "scope": "deterministic — single-node business diff: build-suggest-offer (7972abd8) only",
  "verdict": "PASS (all functional acceptance) + §0 safety gate satisfied by construction (zero executions ran)",
  "promote": "NOT promoted (user-gated). Live spine 9qVyfUxmRQqrpGRMDLRuz UNTOUCHED. No workflow edited by tester.",
  "targets": {
    "clone": {"id": "txiPzSxy3Pclsz6v", "versionId": "ccff92c3-f85a-4707-9144-01f0c7895405", "activeVersionId": "ccff92c3-f85a-4707-9144-01f0c7895405", "draft_eq_active": true},
    "live": {"id": "9qVyfUxmRQqrpGRMDLRuz", "activeVersionId": "a505f2e1-74ef-4fb3-9c87-c4818689b21b", "draft_eq_active": true, "read_only": true}
  },
  "artifact_identity": {
    "node": "build-suggest-offer",
    "node_id_shared_clone_live": "7972abd8-5d6b-40ff-9d38-152782cd8091",
    "node_type": "n8n-nodes-base.code",
    "clone_jsCode_sha256": "2cc445251bbfeaf7fe2ce8041621afeb0054a0da0aedc1f583c6b479d7f95393",
    "coder_claimed_sha256": "2cc44525…95393",
    "sha_match": true,
    "note": "Deployed clone node IS the reviewed coder artifact (sha-gated). live jsCode sha256 00e59cab…9198 (for MT-R comparison).",
    "clone_vs_live_diff": "Behavioral diff confined ENTIRELY to D1 region (lines 163-229). Only other differences: two cosmetic comment-banner box-char lengths (lines 1, 104) + one trailing blank line — none execute. Confirms single-node, D1-only change."
  },
  "mechanism": {
    "type": "offline unit (§MT0 / plan V-MT0). Per UAC §MT0: 'No LLM, no egress; offline unit, egress log empty.'",
    "how": "Extracted the EXACT deployed clone jsCode + fresh live jsCode via read-only get_workflow_details. Ran each in Node with a faithful n8n Code-node env ($input.first().json, $('resolve-entity'), $('Call \\'sub-query-reformulator\\'').first().json.output, $('disallowed-entity-gate'), $('sibling-transform'/'sibling-probe'/'Call \\'sub-get-results\\'').isExecuted=false, $execution.id) built from the §MT0/plan §7 fixture.",
    "executions_run_on_n8n": 0,
    "why_not_test_workflow": "test_workflow pinning would run the full downstream path (compile-current-state, sinked save fork -> RPUSH TEST list) and I am barred from setNodeDisabled to isolate the node. The plan's PRIMARY GATE is explicitly the offline unit; running the exact sha-gated deployed jsCode against pinned inputs is the faithful, truly-zero-egress realization of it.",
    "harness": "/private/tmp/.../scratchpad/tester-harness.js (independent of coder harness); results tester-results.json; negctrl negctrl.js"
  },
  "cases": {
    "MT-1": {
      "verdict": "PASS",
      "asserts_passed": "23/23",
      "suggest_offer": true,
      "suggest_selection_context": "suggest_offer",
      "suggest_response": "Couldn't find some items:\n\n\"C21263XUW-P-ENG\" — did you mean:\n  1. C2181XUW-P-ENG\n  2. C21131XUW-P-ENG\n  3. C21132XUW-P-ENG\n\"Bravat C01014UW-P-ENG\" — did you mean:\n  4. BRCX01014UW-P-ENG\n\"Sorento SRTWCY8605-RL\" — did you mean:\n  5. SRTWCY8605\n  6. SRTWCY8605-PJ\n  7. SRTWC8605-SC-RL\n\nReply a number to pick, or 'yes' to escalate to customer_service.",
      "checks": {
        "3_per_token_blocks": "PASS (A/B/C token labels present)",
        "msg_begin_end": "PASS (begins 'Couldn't find some items:', ends 'escalate to customer_service.')",
        "global_idx_contiguous_1_7": "PASS ([1,2,3,4,5,6,7]; A=1-3 B=4 C=5-7)",
        "suggest_last_result_set_len7_idx_contiguous_values": "PASS (values row1=C2181XUW-P-ENG … row7=SRTWC8605-SC-RL, all entity_type=product)",
        "dym_offer": "PASS ({id:EXEC_MT1, domain:'inventory', ttl:3, candidates:[7], picked:[]})",
        "candidates_for_raw_per_token": "PASS (1-3 C21263XUW-P-ENG, 4 'Bravat C01014UW-P-ENG', 5-7 'Sorento SRTWCY8605-RL'; each code=its candidate; for_hint all 'product')",
        "suggest_quick_reply": "PASS (exactly 'Yes escalate,No it's okay' — NO number buttons)"
      },
      "note": "for_canonical=null on all 7 candidates because the fixture's parser entities are unresolved (no canonical_code); plan §MT-1 acceptance does not require for_canonical non-null. Faithful to an all-miss."
    },
    "MT-R": {
      "verdict": "PASS",
      "hard_gate": "single-token (token-A only) output BYTE-IDENTICAL between changed clone jsCode and current live jsCode",
      "byte_identical": true,
      "output_shape": "code-mode message: 'Couldn't find \"C21263XUW-P-ENG\". Did you mean C2181XUW-P-ENG, C21131XUW-P-ENG, or C21132XUW-P-ENG? Reply with a code to continue, or would you like me to escalate to customer_service team?'; suggest_quick_reply='C2181XUW-P-ENG,C21131XUW-P-ENG,C21132XUW-P-ENG,Yes escalate,No it's okay'; suggest_last_result_set len 3 (label=value=code); dym_candidates len 3 all for_raw:'C21263XUW-P-ENG'.",
      "method": "Same single-token pinned fixture through BOTH jsCodes; JSON.stringify(out) compared — identical. Confirms _survivors.length===1 falls through to the verbatim existing if(d1) block."
    },
    "MT-cap": {
      "verdict": "PASS",
      "fixture": "6 missed product tokens, each with 2 candidates",
      "checks": "exactly 5 per-token blocks rendered (6th token TKF-6 absent); suggest_last_result_set length 10 (<=15); idx contiguous 1..10 no gap"
    },
    "MT-drop": {
      "verdict": "PASS",
      "fixture": "§MT0 but middle token 'Bravat C01014UW-P-ENG' only candidate = bare uuid (no display) -> humanLabel null",
      "checks": "middle token block OMITTED; only 2 blocks (A then C); idx contiguous 1..6 no gap; dym_offer.candidates length 6; none with for_raw:'Bravat C01014UW-P-ENG'"
    },
    "MT-roundtrip": {
      "verdict": "UNVERIFIED (skipped per plan §6 V-MT-roundtrip / UAC §MT-roundtrip explicit provision)",
      "rationale": "E2E regression guard, NOT the gate. output_exchange (fork 847a1173) is UNEDITED by this change; the pick round-trip (NUMBER positional + CODE tryDymPick/for_raw) is pre-existing machinery already covered by the dym-single-use-fix and dym-candidate-map suites. Forcing a real reformulator to emit require_specific:false for a 3-product all-miss is high-cost/high-risk and out of the deterministic gate; UAC §MT-roundtrip explicitly permits skip-and-record-unverified in that case. §MT0 (MT-1) + §MT-R remain the gates and both PASS."
    }
  },
  "negative_control": {
    "purpose": "Prove the §MT-1 assertions are real instruments (can go RED) and that the fix genuinely changes behavior.",
    "method": "Ran the pre-fix LIVE jsCode (has the `break` bug) against the SAME 3-token MT-1 fixture.",
    "result": "Pre-fix output = single-token D1 only ('Couldn't find \"C21263XUW-P-ENG\". Did you mean …'); does NOT start with 'Couldn't find some items:'; suggest_last_result_set length 3 not 7; token B and token C blocks ABSENT. All MT-1 multi-block asserts correctly go RED on pre-fix code.",
    "conclusion": "VALID — assertions fail-on-purpose; the green is not a green-that-cannot-fail."
  },
  "safety_gate_§0": {
    "mode": "offline unit — ZERO n8n executions run by tester (only read-only get_workflow_details). No node executed => no send / assign / SLA / PIC-comment / CRM-PUT / redis-push / LLM call occurred.",
    "S1_zero_real_send": "PASS by construction (no execution). Static: all 8 clone sendmsg callers -> ublq9nSlrpz63xan (sub-sendmsg-CHAT); send-message-files/images/video ORPHAN (0 inbound).",
    "S2_zero_assign_write": "PASS by construction. Static: HI caller -> guarded fork vUfFUDjLAuMaeQE6.",
    "S3_zero_crm_write": "PASS by construction. Static (from clone JSON): save-session-vars + update-human-intervened ORPHAN (0 inbound); Call 'sub-respond-save-message-redis'2.workflowId.value === tWm5DYLxfypmVC1T (TEST fork, NOT live UrETd-jm46tFj3Xw7w8vL). Containment = '5 orphaned + 1 sinked' (matches CLAUDE.md / S3-amendment).",
    "S4_get_results_read_only": "PASS by construction (get-results never invoked). get-results caller -> rysSPgUssLDf6xJc.",
    "S5_test_mode": "N/A — no trigger/sub invoked in an offline unit; deterministic scope, no runtime.",
    "S6_token_sinks": "PASS — deterministic scope, ZERO tokens (no LLM node executed; harness runs pure Code jsCode offline).",
    "S7_prod_ingest_sink_delta": "PASS by construction — no execution touched any redis list; prod ingest list sorento-respond-message and TEST sink sorento-respond-message-TEST both untouched (no save node fired). No before/after LLEN needed because the offline unit performs no redis I/O; there is nothing to attribute.",
    "S8_no_is_test_false_credentialed_run": "N/A — no sub executed at all. Static note: banned egress node types exist in the spine graph (expected — real CRM read httpRequests + orphaned egress), but none fired."
  },
  "findings": [
    "Cosmetic drift clone vs live: two comment-banner box-char lengths differ (lines 1, 104) + a trailing blank line. Non-executing; does not affect MT-R byte-identity of OUTPUT. Flag for promote hygiene (LESSONS §57 — build promote target as LIVE + hunks, not a clone copy; these banner comments would otherwise ride along).",
    "for_canonical is null on MT-1/MT-cap/MT-drop candidates because unresolved parser entities carry no canonical_code. Consistent with an all-miss; not required by acceptance. On a real turn where a miss co-occurs with a resolvable prior entity, for_canonical would populate from _srcEnt — untested here (out of the flagship stock-miss scope).",
    "Theoretical single-token edge NOT tested by MT-R: if a SINGLE miss token's candidates ALL drop via humanLabel, clone (_survivors.length===0 -> d1=null -> D2) and live (d1={token,cands} -> if(d1) with all picks filtered -> falls through to D2) both reach D2, so behavior converges — but via slightly different paths. MT-R fixture has real codes so _survivors.length===1; the all-drop-single edge is not exercised. Low risk (both reach D2/escalate).",
    "MT-roundtrip left UNVERIFIED per plan provision (output_exchange unedited; round-trip machinery already covered by prior suites)."
  ],
  "gate_conclusion": "PRIMARY GATE (§MT0/§MT-1) PASS. HARD REGRESSION GATE (§MT-R byte-identity) PASS. §MT-cap, §MT-drop PASS. Negative control VALID. §0 satisfied. Single-node deterministic change; ready for user-gated promote of build-suggest-offer (7972abd8) only, byte-SHA gated per LESSONS §57/§58, live target built as LIVE+hunks (do NOT copy the clone banner-comment drift)."
}

# Review — `brand-company-routing` (n8n half) · 2026-08-17

Inputs: plan `plans/brand-company-routing-plan.md` (rev-2), UAC `tests/brand-company-routing-UAC.md`, node-diff
`tests/diffs/brand-company-routing.md` (+ byte-exact bodies in `tests/diffs/brand-company-routing/`), tester rollup
`tests/runs/brand-company-routing-rollup-20260817.md` (+ per-case JSON, rev-2 section).

> Process note: this review was performed by the planner seat inline (usage-limit checkpoint; no subagent) — a deviation
> from the named-executor discipline. It is a full read of the diff bodies + evidence, not a rubber stamp; the captain may
> request an independent `sorento-reviewer` pass before promotion.

## Verdict: **APPROVE** (clone build; promotion remains captain-gated)

> Rev-3/rev-4 addendum (review rounds, 2026-08-17): the approval covers the rev-4 bodies committed under
> `tests/diffs/brand-company-routing/` — those files, not the clone, are the reviewed source for the promote. The clone still
> runs rev-2; its republish + re-test (P6, incl. the CHANGED B4/B5 expectations) and the deferred AC8 replay are open items
> in the checklist below.

### 1. Correctness vs plan §3 / captain additions
- (1) `get-cs-members` now carries `brand_code` + `company_id` from `cs-roster-plan` ← `disallowed-entity-gate.routing_companies` — the SAME computed state `compile-current-state` persists and `escalation-context` later sends to `sub-human-intervention` → `next-assignee`. B7 roster parity proves pool identity (Sorento+mocha 6/6, Mocha 3/3, exact ids and order).
- (2) Company is the primary axis: `routing_companies[].company_id` drives one roster call per company; the picked member's `company_id` (from `cs_last_result_set` row) is what the assignment call receives (B3rev2-a/-b). Single-company `routing_company` reaches the bare-"yes" path (B4).
- (3) Multi-company: two calls (B2: Mocha, Sorento+mocha), grouped + labelled reply with the explanation sentence naming both companies, continuous numbering; single-company reply byte-identical shape (B1, B9c). Deviation-1 (Mocha entry brand null rather than inheriting `mocha`) is correct — the Mocha copy has no brand row; borrowing another company's brand would narrow the wrong pool.
- Rev-2 fix (picked member brand = the row's own `brand_code`, null stays null) closes the only pool-mismatch found in rev-1 (B3rev2-a: Mocha member → `brand_code:null`; B3rev2-b: Sorento member → `mocha`).
- **Rev-8 (sixth review round, captain-decided; HI-call params + `compile-current-state`, see diff doc §1g):** (a) the two new `Call 'sub-human-intervention'` inputs are now `isExecuted`-guarded — `$('node').first()` on an unexecuted node throws before `|| ''` applies, so the bare form was safe only while `escalation-context` owned the HI call's single inbound edge (true on the clone, unverified on live); P3a now also enumerates live's inbound edges so a bypassing path is rewired rather than merely survived. (b) The Δ4 merge arm keys its multi-company rendering off `cs_multi_note` (companies queried) like `build-cs-member-offer`, so a two-company offer whose second roster came back empty carries the note AND the `[ … omitted. ]` disclosure on both paths.
- **Rev-7 (fifth review round, captain-decided; `build-cs-member-offer` + `compile-current-state`, see diff doc §1f):** (a) the Δ4 merge arm (date-suggest + member roster in one reply) rebuilt the picker as bare `idx. label`, dropping the company labels and the explanation sentence — criterion (3) held on the plain offer path but not on this one. The arm now labels each line from the row's `companies` and prepends `cs_multi_note`, the single sentence `build-cs-member-offer` exports so both paths cannot drift; single-company output unchanged. (b) The roster-plan carry-forward also requires that no fetch happened this turn: a same-team turn whose roster came back empty used to keep the previous plan and replay its `brand_code` from a superseded fetch.
- **Rev-6 (fourth review round, captain-decided; `build-cs-member-offer` + `compile-current-state`, see diff doc §1e):** membership, not the dedupe winner, decides which companies a member belongs to. Deduped rows carry `company_ids:[…]`; the multi-company renderer groups by that set (one number per member, reused across the groups it appears in) so a company whose CS roster is entirely staff already listed under the first company is no longer declared "no customer-service members are configured" beneath a line labelled with its own name, and `compile-current-state` intersects the roster plan on the same set so a shared-staff two-company offer persists BOTH pairs and the bare "yes" stays on the both-axes-null arm. Non-shared rendering unchanged.
- **Rev-5 (third review round, captain-decided; `compile-current-state` only, see diff doc §1d):** two ways rev-4 recorded `routing_roster_plan` too eagerly. (a) The carry-forward now requires `!_fresh` as well as `_sameTeam` — a turn that resolved a fresh company set without re-fetching a roster used to keep the old plan, which the unpicked arm reads BEFORE `routing_company`, so a stale Sorento plan could outrank a freshly resolved Mocha company and escalate to the wrong CS team. A plan never outlives the roster it describes; fresh resolve wins on every axis. (b) The plan is intersected with the companies that actually contributed an offered member, so a two-company plan whose second roster degraded to empty persists length 1 and the bare-"yes" turn replays that pair verbatim instead of sending both axes null and letting `next-assignee` pick from a company nobody was shown. New UAC cases B5b/B5c cover both.
- **Rev-4 (second review round, captain-decided; repo bodies only — clone republish PENDING, see diff doc §1c):** pool identity now has ONE definition on both arms. `compile-current-state` persists `variables.routing_roster_plan` (the `cs-roster-plan` items `get-cs-members` actually ran with, trimmed to `{plan_idx, company_id, company_name, brand_code}`); `escalation-context`'s bare-"yes" arm sends that single row's pair verbatim, or both axes null when the offer spanned >1 company, and no longer re-derives from `routing_brand`/`query_brands`. This closes the three residual disagreements (multi-company "yes" sending the Sorento-derived `mocha` against an unbranded Mocha roster; a brand stated on the confirmation turn re-narrowing the pool; a stated-brand/zero-company gate turn persisting `routing_brand:null` while the roster HAD been fetched branded). Replay `norm()` for the three generic CRM names is now container-scoped instead of tree-wide. **UAC B4/B5 expectations changed** (B5: `brand_code` null, was `mocha`) — the committed B5rev2 evidence describes rev-2.
- **Rev-3 (review round, captain-decided; repo bodies only — clone republish PENDING, see diff doc §1b):** (a) the pool-identity rule now applies whenever the picked row matched, not only when it carries a `company_id` — the `cs-roster-plan` fallback row could otherwise be re-branded from this turn's `query_brands`; (b) `disallowed-entity-gate` no longer guesses a brand from `access_levels` and only accepts a stated brand when the customer named exactly one, so `routing_brand_source` is `resolved|stated|null` — brand unknown stays unknown and the CRM resolves from the company-bounded base pool; (c) the multi-company sentence is N-safe ("from each of them", is/are). Rev-3 has NOT been re-tested on the clone.
- Report §5.2 items all present: parser fork base `marketing_promotion` + legacy normaliser (P1 real-parser PASS; A4 gate: base set exists in Sorento, 3 members), HI fork inputs/bodies/`test-guard-record` payload, ccs persistence + same-team carry, `escalation-context` guard (B6b: no stale leak on a non-resolving domain switch), replay `norm()` ignore-when-null rule (R1 static).

### 2. Zero-egress safety (§0 S1–S6) — re-confirmed from the tester's evidence
- All 17 + 6 (rev-2) clone executions: sendmsg TEST fork terminal at guard-record; HI fork nodes = trigger/chat?/test-guard/test-guard-record only, `get-round-robin-assignee` never executed; orphaned prod PUT/UPDATE_CONTACT absent; get-results read tools only; session persisted only to `n8n_test.respond_contacts_test`; deterministic turns 0-token; live `9qVyfUxmRQqrpGRMDLRuz` / `XTODTw…` / `rrYXzE61…` unexecuted and versionIds unchanged. `zz-roster-probe` = GET only; `next-assignee` never called.
- Guard topology of the HI fork unchanged (`test-guard` still upstream of every write); the spine clone's orphaned egress nodes still 0-inbound (coder node-set diff). New nodes are pure Code + one read HTTP param change.
- `onError: continueRegularOutput` on `get-cs-members`: turns a roster 404 into an empty-roster fallback (plain offer) instead of a dead turn — safe (read node), and strictly better for the multi-company case. Flagged for promote awareness only.

### 3. Findings (non-blocking)
- F1 B6 UAC wording was corrected in-branch: a same-product re-resolve legitimately refreshes the company (plan §3.5); the guard is proven by B6b.
- F2 B9 UAC trigger example corrected (`tag-not-supported` never sets `is_escalate_offer`).
- F3 Doc drift for CLAUDE.md/AGENTS.md: clone sub wiring (parser `wI5RkNGW3EOJfBdo`, HI `vUfFUDjLAuMaeQE6`, get-results `t4QvrtrPnTwRU6br`, sendmsg `aQUmwMVplmNcyUVc`, save-msg `tWm5DYLxfypmVC1T`); `Call 'sub-respond-save-message-redis'2` on today's clone is not orphaned (TEST sink only). Not part of this change; noted in AGENTS.md pass.
- F4 Live `disallowed-entity-gate` #9 still builds cosmetic `company_team = marketing_promotion_<brand>` for the offer text; harmless with the base key. Out of scope.
- F5 **R1 is DEFERRED, not PASS.** The sample replay was never run; only the static presence of the `norm()` rule was checked, which cannot show the rule suppresses the diffs. AC8 ("a sample replay of ≥3 non-CS golden turns shows no new diffs") is therefore **open** and moves into the promote checklist (P6). The supporting observation — the new keys are null on the non-CS turns that did run (B6b-t2/B9c/P1) — makes the rule likely inert, but it is not the AC8 evidence.
- F6 Assumption A2 (bare "yes" after a multi-company offer → `company_id:null`, CRM resolves via contact/default) is a product choice the captain may want to revisit (alternative: re-prompt for a company). Implemented per plan; **rev-4 extends it to `brand_code:null` on the same arm** — with >1 roster call there is no single pool the offer corresponds to, so neither axis can be sent honestly. After a single-company offer both axes come from the persisted roster-plan pair, so nothing is lost on that path.

### 4. Promote checklist (captain-gated — NOT executed; Lessons 24/25/37)
P0 Backups first: capture live versionIds + the bodies of every target node (`tests/backups/brand-company-routing/LIVE-*` already holds today's rebase sources; refresh before promote).
P1 Parser: live `XTODTw-dJcV0uRdC056hG` › `output_exchange.jsCode` ← `tests/diffs/brand-company-routing/parser-fork-output_exchange.js` (sha `3ee5b658…7eed`; the fork body was rebased on live `67a73561…e2017` — re-verify live sha unchanged first), `AI Agent` systemMessage ← `…/parser-fork-AI-Agent.systemMessage.txt` (`583bcfb0…45f37`; from `0555a9e8…19246`). Draft-sha gate → publish → active-sha gate → auto-revert on mismatch.
P2 HI: live `rrYXzE61gCNUck_zmXe-G`: trigger +`brand_code`,`company_id`; `get-round-robin-assignee` + `conversation-sla-tracking-create` jsonBody per diff doc §2 (**keep live's tracking URL** `…/conversation-sla-tracking`, not the fork's `/integration`); no `test-guard`/`chat?` nodes on live. Publish.
P3 Spine `9qVyfUxmRQqrpGRMDLRuz` (one atomic `update_workflow`): `disallowed-entity-gate` (`5928ae64-…`) jsCode ← `spine-disallowed-entity-gate.js` ONLY IF live sha still `8e1b5470…9b76b`, else re-apply the §3.1 hunk after `out.resolved_companies = _brands;`; add `cs-roster-plan` + rewire `cs-offer-gate[0]→cs-roster-plan→get-cs-members`; `get-cs-members` (`ff5f651e-…`) url/fullResponse/onError; `build-cs-member-offer` whole body; `compile-current-state` — live body differs from the clone (promo-picker S4) → apply the §3.5 hunk before `return output;` (anchor `if (_dymLastResultSet) …` present once), NOT the clone body; add `escalation-context` + rewire `divert-suggest-yes[1]→escalation-context→Call 'sub-human-intervention'` (keep `divert-suggest-yes[1]→tag-out-of-scope`); `Call 'sub-human-intervention'` +2 inputs (workflowId stays live HI; the values are the rev-8 `isExecuted`-guarded expressions, NOT the bare `.first()` form). Publish; verify `versionId==activeVersionId`.
**P3a live-topology gate (do BEFORE the rewire).** Enumerate EVERY inbound connection of the live HI call node
`726da5dc-…` from the live `connections` map — not just `divert-suggest-yes`(`173d3b9f-…`)[1]. Either confirm that edge is
the only one, or route every other inbound edge through `escalation-context` as well. Rationale: the two new inputs
reference `escalation-context`, and an escalation reaching the HI call on a path that skipped it would evaluate a
reference to an unexecuted node. The rev-8 `isExecuted` guard makes that degrade to `''` (CRM resolves from
contact/default) instead of throwing, but a bypassing edge still means an escalation routed with NO company/brand axes —
so it must be found and rewired, not merely survived.
P4 Replay `aROEBlQyyoQaB7a1` norm rule — the orchestrator draft carries the rev-1 tree-wide rule; re-apply the **rev-4
container-scoped** version (`replay-Diff.js`, incl. the `ROUTING_VARS` constant) before relying on it (no publish needed).
P6 **Rev-3 + rev-4 catch-up (do BEFORE P3).** The repo bodies of `spine-escalation-context.js`, `spine-disallowed-entity-gate.js`,
`spine-build-cs-member-offer.js`, `spine-compile-current-state.js`, `spine-cs-roster-plan.js` carry the review fixes (diff doc
§1b + §1c) and the clone does not: republish them on the clone
`txiPzSxy3Pclsz6v`, re-fetch and re-sha (the §5 table's `after` column is rev-2), and re-run UAC B2 (N-safe copy), B3rev2-a/-b
(pool identity, incl. a fallback-item row with `company_id:null` and a conflicting `query_brands` on the pick turn) and a
FULL-access unbranded-product turn (asserting `routing_brand:null`, `routing_brand_source:null`, no `&brand_code=` on the
roster call). Rev-4 adds to that re-run: **B4** (single-company bare "yes" — ccs persisted `variables.routing_roster_plan`
one row, `escalation-context` sends that pair verbatim) and **B5 with its CHANGED expectation** (multi-company bare "yes" ⇒
`brand_code` null, not `mocha`; assert it stays null when the confirmation turn also states a brand), plus the rev-5 cases
**B5b** (a multi-company offer with one empty roster persists a length-1 plan and the bare "yes" replays that pair) and
**B5c** (a fresh resolve without a roster fetch nulls the carried plan, so the next escalation uses the turn-2 company,
not the turn-1 one), the rev-6 shared-staff case (a two-company offer whose second roster is entirely members already
listed under the first: no "no customer-service members" line for that company, both pairs in `routing_roster_plan`,
bare "yes" ⇒ both axes null — **UAC B5d**), **literal B6** (turn 3 must now observe `brand_code:null`), and the rev-7 cases: a
merged date-suggest + two-company member offer (every picker line labelled, `cs_multi_note` present — **UAC B5e**) and a
same-team turn whose roster fetch returned nothing (carried plan dropped, next bare "yes" sends no stale `brand_code` —
**UAC B2b**), plus the rev-8 case: a merged reply on a DEGRADED two-company offer keeps the note and the `[ … omitted. ]`
disclosure (**UAC B5f**). The UAC table and this list are the same set. Also run the deferred
AC8 check here: replay `turn_limit=3` over non-CS golden turns → zero new `replay_node_diffs` (F5), which now also covers
the container-scoped norm rule and the new `routing_roster_plan` key. Promote only after these pass.
P5 Post-promote watch: first real CS escalation → `next-assignee` response `company_id`/`brand_code`/`team_set_code` echo matches the offer; revert trigger = any 404 "No team found … in company" on `next-assignee` (means a company lacks the `customer_service` set — admin config, not code).

# === Secrets (NEVER commit) ===
# .env = connection facts + creds
.env
.env.*
# .mcp.json holds a live n8n MCP Bearer JWT
.mcp.json
# GCP service-account key
teh-jayson-*.json
*-service-account*.json

# === Docker runtime volumes (bind-mounted state, huge, machine-local) ===
mongodb_data/
mongodb_config/
n8n_data/
postgres_data/
redis_data/

# === Large DB dumps / data (not source) ===
# ~729MB pg dump
sorento_embedding.sql
*.dump

# === Claude Code machine-local overrides ===
.claude/settings.local.json

# === OS / editor cruft ===
.DS_Store
*.swp

# PII pseudonymisation map for test fixtures — never commit
n8n-workflows-init/.pii-scrub-map.json

# deploy.py gate (e) pre-write backups — machine-local, dirties the tree if tracked
n8n-workflows-init/tests/backups/*.json

# prod deploy backups (written by scripts/deploy.sh on the prod box)
scripts/backups/

# per-instance credential alias overrides (names only, no secrets) — example is committed
scripts/cred-aliases.json
